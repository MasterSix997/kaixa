# Features, optional dependencies, and definitions

```sh
kaixa generate
kaixa run

kaixa generate --config diagnostics
kaixa run --config diagnostics

kaixa generate --config full
kaixa run --config full
```

Use `kaixa inspect packages --verbose --config diagnostics` to see the activated dependency and features.
